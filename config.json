{
  "config": {
    "cpu": true,
    "gpu": true,
    "memory": true,
    "storage": true,
    "network": true
  },
  "profile": {
    "hostname": "Nils",
    "description": "Default profile for all users."
  }
}